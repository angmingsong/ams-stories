<section class="about-links">
  <a href={`${base}/conversations`}>See where I've been</a>
  <a href={`${base}/connect`}>Share what stayed with you</a>
</section>
```

And add `const base = import.meta.env.BASE_URL;` in the frontmatter at the top (between the `---` lines), after the import line.

Save all files. Then push:
```
git add .
```
```
git commit -m "fix css and link paths"
```
```
git push
